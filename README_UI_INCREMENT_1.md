# UI Increment 1 — stable d22cd25 baseline

This update is intentionally frontend-only.

Changes:
- General Process data/sample selection now appears immediately after Workflow Class.
- The existing demo-model button is grouped with General Process data loading.
- Baseline simulation / automated optimization controls are moved below data/model loading and labeled "Baseline & analysis".
- Existing Contact Center data import remains directly below Workflow Class when Contact Center is selected.
- Routing probability inputs display at most 3 decimal places and use step 0.001.
- Existing API paths, request handlers, simulation logic, calibration logic, optimization logic, and Manual SVD logic are unchanged.

Base: d22cd25
