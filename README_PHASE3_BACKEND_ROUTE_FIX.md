# Phase 3 backend route fix

Adds the backend implementation and FastAPI route required by the existing Phase 3 Scheduling Policy Matrix UI.

Files:
- backend/api/index.py
- backend/api/core/scheduling.py

No frontend files are changed, so the current overflow-sensitivity UI remains intact.
